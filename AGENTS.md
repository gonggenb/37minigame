# AGENTS.md

## 1. 读取顺序

Codex 每次执行任务前，必须先阅读本文件。

如果任务涉及玩法、代码实现、Unity 场景、Prefab、UI 或系统调整，还需要继续阅读：

1. `docs/project_core.md`
2. `docs/gameplay_systems.md`
3. `docs/unity_tech.md`
4. `docs/codex_workflow.md`

如果任务是在要求推进下一步开发、拆分任务、修复问题或生成提示词，还需要额外阅读：

5. `docs/task_prompts.md`

`docs/design_plan.md` 是完整策划案，可在需要理解整体设计时阅读，但日常执行优先以上文件。

## 2. 任务分类与 MCP 调用边界

每次开始工作前，必须先把当前需求归入以下一种任务类型。一次任务只能执行一种类型，不得把脚本开发、Unity Editor 操作和运行测试混在同一轮中。

### 2.1 脚本开发任务

脚本开发任务只允许通过终端和文件读写工具处理源码与文档，包括创建、修改、删除脚本、测试脚本、配置文本和开发文档。

脚本开发任务中禁止：

- 调用任何 Unity MCP 工具。
- 修改 Scene、Prefab、GameObject、Component 或 Inspector 绑定。
- 启动 Unity Play Mode、运行游戏或执行运行态测试。
- 把“脚本已经生成”描述成“组件已经挂载”或“功能已经在场景中生效”。

脚本完成后必须停止，列出后续需要单独执行的 Editor 工作或测试工作，等待用户发起下一阶段。

### 2.2 Editor 工作任务

Editor 工作任务用于创建或调整 Scene、Prefab、GameObject、Component、Inspector 引用、资源导入设置等 Unity Editor 状态。

Editor 工作任务原则上通过 Unity MCP 完成。调用任何 MCP 工具前，必须先向用户提出审批，并明确告知：

- 当前任务类型为 Editor 工作任务。
- 为什么必须使用 MCP。
- 计划调用的 MCP 工具或能力。
- 准备操作的 Scene、Prefab、GameObject、Component 或资源。
- 预期结果和可能影响。

只有获得用户明确同意后才能调用。每次实际调用前还要告知本次调用的工具和目的；调用后要报告结果。未经审批不得先读取 Editor 状态、层级、Console 或其他 MCP 资源。

Editor 工作任务只完成编辑器配置，不进入 Play Mode，不顺带执行游戏测试。完成后必须停止并等待用户单独发起测试任务。

### 2.3 测试任务

测试任务用于通过 Unity MCP 启动或控制 Play Mode、运行 Unity 测试、读取 Console、检查场景运行状态和验证当前游戏表现。

测试任务调用任何 MCP 工具前，同样必须先提出审批，说明计划调用的工具、测试场景、测试步骤、观察指标和可能影响；获得用户明确同意后才能执行。每次实际调用前告知工具与目的，调用后报告结果、日志和未通过项。

测试任务只验证当前版本，不修改脚本、Scene、Prefab、Inspector 或资源配置。发现问题时先记录并报告，不得在同一任务中直接转入修复。

### 2.4 多阶段需求

如果一个需求同时包含脚本、Editor 配置和测试，必须拆分并按以下顺序推进：

1. 脚本开发任务。
2. Editor 工作任务。
3. 测试任务。

每个阶段都作为独立任务执行，阶段结束后停止并汇报，等待用户明确要求进入下一阶段。不得为了“一次完成”连续跨阶段调用 MCP。

如果任务类型不明确或范围同时落入多种类型，必须先向用户确认本轮只执行哪一种类型。

### 2.5 电脑控制限制

不得使用电脑控制工具代替 Unity MCP 操作编辑器。如果需求必须依赖鼠标、键盘或其他桌面交互，应列出清晰的手动步骤并请用户完成，不得自行控制电脑。

## 3. 项目一句话目标

本项目是一款使用 Unity 制作的“小体量、可反复游玩的 1 分钟武侠 Roguelite 自动战斗游戏”。

玩家在 60 秒主地图阶段中探索、碰怪、进入自动战斗、获得成长、寻找隐藏洞穴，并在时间结束后进入最终 Boss 战。

当前首要目标不是做完整商业游戏，而是先跑通一个可反复试玩的小型核心闭环。

## 4. 三条硬规则

以下规则不得擅自修改：

1. 普通地图碰怪战斗时，主地图 60 秒倒计时继续流逝。
2. 隐藏洞穴内战斗时，主地图 60 秒倒计时暂停。
3. 最终 Boss 战不受 60 秒限制，作为独立阶段单独计算 Boss 战时间。

## 5. Unity 制作约束

本项目使用 Unity 制作，默认使用 C# 编写游戏逻辑。

Codex 不得擅自：

- 更换游戏引擎。
- 改成 Godot、Unreal、Cocos 或其他引擎方案。
- 引入与 Unity 项目无关的工程结构。
- 假装已经完成 Unity Editor 中的手动绑定。
- 接入真实广告 SDK、支付 SDK、后端服务、账号系统、联机系统或排行榜系统。

如果某些内容需要在 Unity Editor 中手动完成，必须明确说明：

- 需要创建哪些 GameObject。
- 需要挂载哪些脚本。
- Inspector 需要绑定哪些字段。
- 需要拖入哪些 Prefab、UI、Audio 或配置引用。
- 如何进入 Play Mode 验证。

## 6. 当前开发优先级

优先级从高到低：

1. 跑通核心玩法闭环。
2. 保证 Unity 项目可运行、可编译、可测试、可反复试玩。
3. 再增加武学、敌人、洞穴、奖励、事件等内容。
4. 最后再做 UI 美化、音效、正式美术、商业化预留。

## 7. 当前不要做

除非用户明确要求，否则不要做：

- 真实广告 SDK。
- 真实支付 SDK。
- 后端服务器。
- 账号系统。
- 联机系统。
- 排行榜系统。
- 云存档。
- 大型剧情系统。
- 大型装备词条系统。
- 大规模正式美术接入。
- 大范围重构。
- 更换 Unity 技术路线。
- 更换主要编程语言。

## 8. 完成任务后的回复要求

Codex 完成任务后，需要明确回复：

- 本次任务类型。
- 修改了哪些文件。
- 新增了哪些文件。
- 实现了什么功能。
- 是否调用 MCP；若调用，列出审批情况、工具与结果。
- 哪些内容需要在 Unity Editor 手动绑定。
- 是否符合三条核心时间规则。
- 如何运行。
- 如何测试。
- 还有哪些未完成事项。

不要只回复“已完成”。

## 9. 核心执行原则

如果任务目标和本文件冲突，以本文件为准。

如果用户的新指令明确覆盖旧规则，以用户最新明确指令为准。

如果不确定，不要擅自扩大范围，先基于当前 Unity 仓库结构做最小、安全、可运行的实现。

